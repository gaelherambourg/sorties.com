<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}premier essai!{% endblock %}</title>
        <script src="https://code.jquery.com/jquery-3.5.1.min.js"
                integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
                crossorigin="anonymous">
        </script>
        {% block stylesheets %}{% endblock %}


        {% block javascripts %}{% endblock %}
    </head>
    <body>

    {% if app.user %}
        <a href="{{ path('AccueilSorties') }}" title="Accueil">Accueil</a>
        <a href="{{ path('modif_profil') }}" title="profil utilisateur">Mon profil</a>
        <a href="{{ path('app_logout') }}" title="deconnection">Se déconnecter</a>
        <div>
            Bonjour, {{app.user.pseudo}}
        </div>
    {% else %}
        <a href="{{ path('app_login') }}" title="identification">S'identifier</a>
        <a href="{{ path('app_register') }}" title="inscription">S'incrire</a>
    {% endif %}

        {% block body %}{% endblock %}
    </body>
</html>



